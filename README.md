# YMM4素材同梱プラグイン

YMM4素材同梱プラグインは、YukkuriMovieMaker4 のプロジェクト素材をまとめて管理・共有するためのプラグインです。  
特定のファイルを除外したり、素材をパッケージ化して他のユーザーと同じ環境で編集を再開できます。

## 概要

- YukkuriMovieMaker プロジェクト内の素材を簡単にまとめたり展開可能 
- プロジェクトファイル内の設定を削除する機能も提供
- 除外設定ウィンドウで不要なファイルを選択的に除外可能  
- YMMPXライブラリを用いたプロジェクトのパッケージ化と展開機能を提供
- プロジェクトファイルの関連付け機能で、.ymmpx ファイルをダブルクリックで展開＆再リンクして開くことが可能

---

## 主な機能

### プロジェクト素材のパッケージ化

- プロジェクト内の素材を一括でパッケージ化  
- 他のユーザーと共有可能  
- 除外設定で指定したファイルはパッケージ化から除外されます  
- ウインドウ配置などの不要な内部情報を削除可能

### 除外設定

- パッケージ化したくないファイルをチェックボックスで簡単に除外  
- 「すべて選択」「すべて解除」ボタンで一括操作  
- OK で設定を保存、キャンセルで変更を破棄  
- 注:著作権で保護された素材は除外してください
 
---

## インストール方法

1. リリースから最新の `.ymme` をダウンロード  
2. ダウンロードした `.ymme` を起動してインストール  
3. ツールから「素材同梱プラグイン」を起動  

使用時にライブラリのインストールが必要な場合は、自動でYMMEがダウンロード、起動されるのでウインドウに沿って操作してください。

---

## 使用方法

1. YukkuriMovieMaker を起動  
2. ツールタブから「素材同梱プラグイン」を選択  
3. 除外設定ウィンドウを開き、除外したいファイルにチェック  
4. OK ボタンで保存  
5. 必要に応じてプロジェクト全体をパッケージ化  

---

## v0.3.1 / v1.0.0 移行時の注意

- 旧構成の `YmmpxLib` フォルダーがプラグインルート配下に残っている場合、起動時に警告が表示されます  
- 現行バージョンでは旧 `YmmpxLib` フォルダーは不要です  
- 旧フォルダーが残っていると古いライブラリと競合し、パッケージ化・展開が失敗する可能性があります  
- YMM終了後に `YmmpxLib` フォルダーのみを削除してください（自動削除は行いません）  

---

## 出力ファイルの例

| 種類 | 説明 |
| ---- | ---- |
| **元のプロジェクト** | `C:\Users\<ユーザー名>\Videos\YMMProjects\MyProject.ymmp` |
| **出力先ディレクトリ** | `C:\Users\<ユーザー名>\Videos\YMMProjects\` |
| **生成されるファイル** | `C:\Users\<ユーザー名>\Videos\YMMProjects\MyProject.ymmpx` |


## ymmpx ファイルの関連付け

- `.ymmpx` ファイルをダブルクリックで自動的に展開できるように、  
  「ファイル関連付け」機能を利用できます。  
- プラグインのメイン画面で **「.ymmpx 関連付けを設定」ボタン** をクリックすると、  
  `YMMResourceUnpackerApp` が連携モードで起動し、成功/失敗を画面に表示します。  
- 設定後は `.ymmpx` ファイルをダブルクリックで開くだけで、  
  プロジェクトと素材が展開＆再リンクされ開かれます。

---

## 注意事項

- 除外したファイルはパッケージ化対象から外れますが、プロジェクト内にはリンクのみ残ります   
- プラグインの動作には YMMPXライブラリが必要  
- 著作権で保護された素材や再配布不可素材は、除外設定で必ず除外してください  
- 不具合や要望は、GitHub リポジトリ、Discord(hazimeyou)、もしくは作者の X（[@hazimeyou0922](https://x.com/hazimeyou0922)）で報告してください  
- プラグインの使用は自己責任で行ってください  
- プラグインの利用によって発生した損害について、作者は一切責任を負いません  

---

## Release Notes (v0.3.1 / v1.0.0)

- 旧 `YmmpxLib` フォルダー検出時に削除案内を表示
- 旧 `YmmpxLib` フォルダーを開く導線を追加（自動削除はしない）
- パッケージ化前の素材チェック（検出素材数・除外数・見つからない素材数）を表示
- 完了時サマリー表示を追加
- 出力先同名ファイル処理と失敗時の中途半端な `.ymmpx` 残留防止を強化

## ライセンス

このプロジェクトは [MIT License](./LICENSE) のもとで公開されています。

YMMPXアイコンの文字表現には、あずきフォント制作の「うずらフォント」を使用しています。
